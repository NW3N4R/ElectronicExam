<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="ElectronicExam.Administrator.Uploaders.NewExamQuestion"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:ElectronicExam.Administrator.Uploaders"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      Loaded="Page_Loaded"
      xmlns:conv ="using:ElectronicExam.Administrator.valueConverters"
      mc:Ignorable="d">
    <Page.Resources>
        <conv:StringToByteConverter x:Key="toByte" />
    </Page.Resources>
    <Grid RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid ColumnSpacing="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"
                                  MinWidth="120" />
                <ColumnDefinition Width="auto"
                                  MinWidth="120" />
                <ColumnDefinition Width="auto"
                                  MinWidth="120" />
            </Grid.ColumnDefinitions>
            <ComboBox x:Name="SelectExamHeader"
                      SelectionChanged="SelectExamHeader_SelectionChanged"
                      Header="Select an Exam"
                      SelectedIndex="0"
                      SelectedValuePath="id"
                      DisplayMemberPath="SubjectName"
                      HorizontalAlignment="Stretch" />
            <Button x:Name="AddQuestion"
                    Click="AddQuestion_Click"
                    Grid.Column="1"
                    VerticalAlignment="Bottom"
                    HorizontalAlignment="Stretch">
                <StackPanel Orientation="Horizontal"
                            Spacing="10"
                            VerticalAlignment="Center">
                    <SymbolIcon Symbol="Add" />
                    <TextBlock Text="New Question" />
                </StackPanel>
            </Button>
            <Button x:Name="SaveQuestions"
                    Click="SaveQuestions_Click"
                    Grid.Column="2"
                    VerticalAlignment="Bottom"
                    HorizontalAlignment="Stretch">
                <StackPanel Orientation="Horizontal"
                            Spacing="10"
                            VerticalAlignment="Center">
                    <SymbolIcon Symbol="Accept" />
                    <TextBlock Text="Save" />
                </StackPanel>
            </Button>
        </Grid>
        <Grid Grid.Row="1"
              ColumnSpacing="15"
              Padding="20,0,20,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="ExamTitle"
                       Grid.Column="0"
                       VerticalAlignment="Center" />
            <TextBlock x:Name="ExamTeacher"
                       Grid.Column="1"
                       VerticalAlignment="Center" />
            <TextBlock x:Name="ExamSubject"
                       Grid.Column="2"
                       VerticalAlignment="Center" />
        </Grid>
        <ListView x:Name="QuestionsView"
                  SelectionMode="None"
                  Grid.Row="2">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Expander HorizontalAlignment="Stretch"
                              HorizontalContentAlignment="Stretch"
                              Expanding="Expander_Expanding"
                              IsExpanded="{Binding isExpanded,Mode=TwoWay}">
                        <Expander.Header>
                            <Grid ColumnSpacing="10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="auto" />
                                    <ColumnDefinition Width="auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Question"
                                           VerticalAlignment="Center" />
                                <TextBlock Text="{Binding QNo}"
                                           Grid.Column="1"
                                           VerticalAlignment="Center" />
                                <AppBarButton Icon="Cancel"
                                              x:Name="RemoveQuestion"
                                              Tag="{Binding}"
                                              Click="RemoveQuestion_Click"
                                              LabelPosition="Collapsed"
                                              Grid.Column="2"
                                              HorizontalAlignment="Right" />
                            </Grid>
                        </Expander.Header>
                        <Expander.Content>
                            <Grid Padding="10"
                                  CornerRadius="10"
                                  RowSpacing="10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="auto" />
                                    <RowDefinition Height="auto" />
                                    <RowDefinition Height="auto" />
                                    <RowDefinition Height="auto" />
                                </Grid.RowDefinitions>
                                <TextBox Header="Question"
                                         PlaceholderText="The Question?"
                                         Text="{Binding Question,Mode=TwoWay}" />
                                <TextBox Header="Explaining"
                                         PlaceholderText="Explain The Question Here"
                                         Text="{Binding Explaining,Mode=TwoWay}"
                                         Grid.Row="1" />
                                <Grid Grid.Row="2"
                                      ColumnSpacing="10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBox Header="Answeer A"
                                             PlaceholderText="Answeer A"
                                             Text="{Binding AnsA,Mode=TwoWay}" />
                                    <TextBox Header="Answeer B"
                                             PlaceholderText="Answeer B"
                                             Text="{Binding AnsB,Mode=TwoWay}"
                                             Grid.Column="1" />
                                    <TextBox Header="Answeer C"
                                             PlaceholderText="Answeer C"
                                             Text="{Binding AnsC,Mode=TwoWay}"
                                             Grid.Column="2" />
                                    <TextBox Header="Answeer D"
                                             PlaceholderText="Answeer D"
                                             Text="{Binding AnsD,Mode=TwoWay}"
                                             Grid.Column="3" />
                                </Grid>
                                <Grid Grid.Row="3"
                                      ColumnSpacing="20">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <ComboBox Header="Correct Answeer"
                                              SelectedIndex="0"
                                              HorizontalAlignment="Stretch"
                                              SelectionChanged="ComboBox_SelectionChanged">
                                        <x:String>A</x:String>
                                        <x:String>B</x:String>
                                        <x:String>C</x:String>
                                        <x:String>D</x:String>
                                    </ComboBox>
                                    <TextBox Header="Mark"
                                             PlaceholderText="Mark"
                                             Grid.Column="1"
                                             Text="{Binding Mark,Mode=TwoWay,Converter={StaticResource toByte}}" />
                                </Grid>
                            </Grid>
                        </Expander.Content>
                    </Expander>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
