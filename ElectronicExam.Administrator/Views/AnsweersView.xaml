<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="ElectronicExam.Administrator.Views.AnsweersView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:ElectronicExam.Administrator.Views"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      Loaded="Page_Loaded"
      mc:Ignorable="d">

    <Grid RowSpacing="10"
          MinWidth="260">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="320" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal"
                    Spacing="10">
            <TextBlock Text="{Binding Student.Code}"
                       Style="{ThemeResource SubtitleTextBlockStyle}" />
            <TextBlock Text="{Binding Student.fullName}"
                       Style="{ThemeResource SubtitleTextBlockStyle}" />
         
        </StackPanel>
        <ListView ItemsSource="{Binding answeersList}"
                  Grid.Row="1"
                  SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Padding="15"
                                Margin="0,0,0,5"
                                Background="{StaticResource CardBackgroundFillColorSecondaryBrush}"
                                CornerRadius="10">
                        <TextBlock Text="{Binding Question}" />
                        <TextBlock Text="{Binding Mark}" />
                        <TextBlock Text="{Binding Status}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
