<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="ElectronicExam.Administrator.Views.ExamsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:ElectronicExam.Administrator.Views"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:NewExam="using:ElectronicExam.Administrator.Uploaders"
      mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid ColumnSpacing="10"
              Padding="10,5,10,5">
            <Button Content="New Exam">
                <Button.Flyout>
                    <Flyout>
                        <NewExam:NewExam />
                    </Flyout>
                </Button.Flyout>
            </Button>
        </Grid>
        <ListView x:Name="mylist"
                  x:FieldModifier="Public"
                  Grid.Row="1">
            <ListView.Header>
                <Grid ColumnSpacing="10"
                      Padding="10,5,10,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Teacher Name" />
                    <TextBlock Text="Subject Name"
                               Grid.Column="1" />
                    <TextBlock Text="Title"
                               Grid.Column="2" />
                    <TextBlock Text="Date"
                               Grid.Column="3" />
                    <TextBlock Text="Time"
                               Grid.Column="4" />
                </Grid>
            </ListView.Header>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid ColumnSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding TeacherName}" />
                        <TextBlock Text="{Binding SubjectName}"
                                   Grid.Column="1" />
                        <TextBlock Text="{Binding Title}"
                                   Grid.Column="2" />
                        <TextBlock Text="{Binding getDate}"
                                   Grid.Column="3" />
                        <TextBlock Text="{Binding ETime}"
                                   Grid.Column="4" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
