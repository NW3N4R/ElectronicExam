<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="ElectronicExam.Administrator.Views.ExamsResultView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:ElectronicExam.Administrator.Views"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      Loaded="Page_Loaded"
      mc:Ignorable="d">
    <Grid MinWidth="520">
        <ListView ItemsSource="{Binding StudentResultList}"
                  SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="4"
                          Padding="10"
                          Background="{ThemeResource CardBackgroundFillColorSecondary}"
                          CornerRadius="10"
                          RowSpacing="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto" />
                            <RowDefinition Height="auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal"
                                    Spacing="10">
                            <TextBlock Text="{Binding Code}"
                                       Foreground="{StaticResource SystemAccentColor}" />
                            <TextBlock Text="{Binding fullName}" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal"
                                    Spacing="10"
                                    Grid.Row="1">
                            <TextBlock Text="{Binding Status}" />
                            <TextBlock Text="{Binding TotalMark}" />
                        </StackPanel>
                        <Button Content="see answeers"
                                Tag="{Binding id}"
                                x:Name="ShowStudentsAnsweers"
                                Click="ShowStudentsAnsweers_Click"
                                Grid.Column="1"
                                Grid.Row="1"
                                HorizontalAlignment="Right"
                                Background="Transparent"
                                BorderBrush="Transparent"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
