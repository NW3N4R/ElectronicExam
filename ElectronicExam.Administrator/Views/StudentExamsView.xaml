<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="ElectronicExam.Administrator.Views.StudentExamsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:ElectronicExam.Administrator.Views"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Padding="10"
                    Margin="15,0,15,10"
                    CornerRadius="10"
                    VerticalAlignment="Center"
                    Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                    Orientation="Horizontal" Spacing="20">
            <StackPanel>
                <TextBlock Text="{Binding TotalExamsJoined}"
                           TextAlignment="Center"
                           Style="{ThemeResource SubtitleTextBlockStyle}" />
                <TextBlock Text="Total Exams Joined"
                           TextAlignment="Center" />
            </StackPanel>
            <StackPanel>
                <TextBlock Text="{Binding ExamsPassed}"
                           TextAlignment="Center"
                           Style="{ThemeResource SubtitleTextBlockStyle}" />
                <TextBlock Text="Exams Passed"
                           TextAlignment="Center" />
            </StackPanel>
            <StackPanel>
                <TextBlock Text="{Binding ExamsFailed}"
                           TextAlignment="Center"
                           Style="{ThemeResource SubtitleTextBlockStyle}" />
                <TextBlock Text="Exams Failed"
                           TextAlignment="Center" />
            </StackPanel>
        </StackPanel>
        <ListView Grid.Row="1"
                  ItemsSource="{Binding ExamsResultsList}"
                  SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="10"
                          Margin="0,0,0,10"
                          CornerRadius="10"
                          Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto" />
                            <RowDefinition Height="auto" />
                            <RowDefinition Height="auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Title}"
                                   Style="{ThemeResource TitleLargeTextBlockStyle}" />
                        <StackPanel Grid.Column="2"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center"
                                    Grid.RowSpan="3">
                            <TextBlock Text="{Binding TokenMark}"
                                       Style="{ThemeResource SubheaderTextBlockStyle}"
                                       Grid.Row="2" />
                            <Button Background="Transparent"
                                    BorderBrush="Transparent"
                                    Tag="{Binding id}"
                                    Content="see Answeers"
                                    x:Name="SeeAnsweers"
                                    Click="SeeAnsweers_Click" />
                        </StackPanel>
                        <TextBlock Text="{Binding Status}"
                                   Style="{ThemeResource TitleTextBlockStyle}"
                                   Grid.Column="0"
                                   Grid.Row="2" />

                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
